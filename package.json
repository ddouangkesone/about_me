{
  "name": "about_me",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "postcss": "^8.5.1",
    "postcss-normalize": "^13.0.1",
    "postcss-preset-env": "^10.1.3",
    "postcss-rtl": "^2.0.0",
    "rc-progress": "^4.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^5.0.1",
    "react-scroll-trigger": "^0.6.14",
    "tachyons": "^4.11.1",
    "tachyons-cli": "^1.3.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "css": "tachyons src/css/app.css > src/index.css --minify",
    "prestart": "npm run css",
    "prebuild": "npm run css"
  },
  "homepage": "http://ddouangkesone.github.io/about_me",
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "gh-pages": "^2.0.1"
  }
}
